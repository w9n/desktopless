onboot:
  - name: modprobe
    image: linuxkit/modprobe:<latest>
    command: ["modprobe", "ext4"]
  - name: format
    image: linuxkit/format:<latest>
  - name: mounts
    image: linuxkit/mount:<latest>
    command: ["/usr/bin/mountie", "/var/lib/"]
trust:
  org:
    - linuxkit
